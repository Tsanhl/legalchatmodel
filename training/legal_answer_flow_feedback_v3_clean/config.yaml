# Clean retraining run: the dataset is rebuilt from the base legal-answer data
# and only complete, user-supplied replacement answers are allowed in later.
model: "mlx-community/Qwen2.5-7B-Instruct-Uncensored-4bit"
train: true
test: false

fine_tune_type: lora
optimizer: adamw
data: "data/legal_answer_flow_feedback_v2"
adapter_path: "adapters/legal_answer_flow_feedback_v3_clean_lora"
resume_adapter_file: "adapters/legal_answer_flow_auto_lora_400/adapters.safetensors"

seed: 23
num_layers: 16
batch_size: 1
grad_accumulation_steps: 4
iters: 100
learning_rate: 5e-6

mask_prompt: true
max_seq_length: 4096
grad_checkpoint: true

steps_per_report: 10
steps_per_eval: 25
val_batches: 16
save_every: 50
test_batches: -1

lora_parameters:
  keys: ["self_attn.q_proj", "self_attn.v_proj"]
  rank: 8
  scale: 20.0
  dropout: 0.0
